#include<stdio.h>//pre processed things
int main()//entry point
{
    if(1)
        printf("A");
    if(0)
        printf("B");
    if(17)
        printf("C");
    if(-81)
        printf("D");

    return 0;
}

